package com.jobportal.applicationservice.config;

// Intentionally empty to avoid conflicting RestControllerAdvice beans.
final class GlobalExceptionHandler {
    private GlobalExceptionHandler() {
        // no-op
    }
}
